{
  "items": [
    {
      "file": "src/lib/sources/alphavantage.ts",
      "kind": "dead-code",
      "before": "export async function fetchPutCallRatio(apiKey: string, symbol = \"SPY\"): Promise<number> { ... } — REALTIME_PUT_CALL_RATIO 호출용 함수",
      "after": "삭제됨",
      "reason": "ts-prune이 미사용으로 표시(태그 없음). grep 결과 정의부 외 호출처 0건(src/, scripts/ 전체). pipeline.ts는 같은 파일의 fetchHistoricalPutCallRatio만 사용하고 있어(실시간이 아니라 과거 날짜 버전으로 전환된 것으로 보임) 이 realtime 버전은 완전히 고아 상태. PutCallRatioResponse 인터페이스는 fetchHistoricalPutCallRatio가 계속 쓰므로 유지."
    },
    {
      "file": "src/lib/news-events.ts",
      "kind": "dead-code",
      "before": "import { newsItemWeight, capDailyHighSeverity, ... } from \"./news-severity\"; — newsItemWeight를 이 파일에서 import",
      "after": "newsItemWeight import 제거(나머지 import는 유지)",
      "reason": "eslint no-unused-vars가 8행 newsItemWeight를 미사용으로 표시. grep 결과 이 파일 안에서 newsItemWeight를 실제로 참조하는 코드가 없음(news-severity.ts와 scoring/run.ts에서만 직접 사용). 같은 파일의 `export * from \"./news-severity\"`가 이미 재수출하므로 외부에서 news-events.ts를 통해 newsItemWeight를 import하는 경로는 그대로 살아있음 — 이 named import 바인딩 자체만 죽은 코드."
    },
    {
      "file": "src/lib/sources/types.ts",
      "kind": "dead-code",
      "before": "export type MetricId = (typeof METRICS)[keyof typeof METRICS];",
      "after": "삭제됨",
      "reason": "ts-prune이 미사용으로 표시(태그 없음, 모듈 내부 사용도 없음). grep -rn \"MetricId\" 전체 리포지토리 결과 정의부 1건 외 0건."
    },
    {
      "file": "src/lib/sources/types.ts",
      "kind": "dead-code",
      "before": "export interface SourceFetchResult { points: FetchedPoint[]; errors: { metric: string; message: string }[]; }",
      "after": "삭제됨",
      "reason": "ts-prune이 미사용으로 표시(태그 없음, 모듈 내부 사용도 없음). grep -rn \"SourceFetchResult\" 전체 리포지토리 결과 정의부 1건 외 0건."
    },
    {
      "file": "scripts/simulate-step24-redesign.ts",
      "kind": "dead-code",
      "before": "const s8 = r.step8 as unknown as Step8Result; (할당만 되고 이후 전혀 참조 안 됨), import type { Step8Result } from \"../src/lib/scoring/types\";",
      "after": "두 줄 다 삭제됨(s8 변수 + 이제 미사용이 된 Step8Result 타입 import)",
      "reason": "eslint no-unused-vars가 16행 's8'을 미사용으로 표시. grep 결과 파일 전체에서 s8이 정의 줄 외 재참조 없음을 확인. 이 스크립트는 읽기 전용 1회성 분석 스크립트(scoring 엔진 코드 자체가 아님)라 삭제 범위에 해당."
    },
    {
      "file": "src/lib/verdict-outcomes.ts",
      "kind": "skipped",
      "before": "export const WATCH_NEUTRAL_BAND_PCT = NEUTRAL_BAND_PCT; (10행, @deprecated 주석: \"NEUTRAL_BAND_PCT로 통합됨(예전엔 지켜보기에만 적용됐다). 외부 참조 호환용.\")",
      "after": "변경 없음",
      "reason": "ts-prune이 미사용으로 표시했고 grep으로도 리포지토리 내 참조 0건 확인했지만, 코드 자체에 '외부 참조 호환용'이라는 명시적 deprecated 주석이 있어 이 리포 바깥(예: 다른 프로젝트나 과거 export 데이터)에서 참조될 가능성을 배제할 수 없음 — 애매하면 삭제하지 말라는 원칙에 따라 보류."
    },
    {
      "file": "src/lib/news-severity.ts (MAX_ITEM_WEIGHT / RISK_ITEM_CAP / MAX_HIGH_PER_DAY)",
      "kind": "skipped",
      "before": "ts-prune 출력엔 \"src/lib/news-events.ts:36/40/94\"로 표시됨(news-events.ts의 `export * from \"./news-severity\"` 배럴 재수출 때문에 ts-prune이 원본 파일 대신 재수출 지점 경로를 보고함)",
      "after": "변경 없음",
      "reason": "grep 확인 결과 세 상수 모두 news-severity.ts 내부에서 실제로 계산에 쓰이고 있고(MAX_ITEM_WEIGHT/RISK_ITEM_CAP은 46~59행 정규화 계산, MAX_HIGH_PER_DAY는 94~106행 상한 로직), MAX_HIGH_PER_DAY는 news-severity.test.ts가 외부에서 직접 import해서 검증까지 함. ts-prune이 배럴 파일 재수출을 원본 정의로 오인한 false positive."
    },
    {
      "file": "src/lib/scoring/ppt-slides.ts",
      "kind": "skipped",
      "before": "33행 'step7' is assigned a value but never used (eslint no-unused-vars)",
      "after": "변경 없음",
      "reason": "src/lib/scoring/** 는 이번 태스크 범위에서 명시적으로 제외된 스코어링 엔진 내부 코드라 손대지 않음."
    },
    {
      "file": "다수 파일 (src/app/**/page.tsx, layout.tsx, route.ts 등)",
      "kind": "skipped",
      "before": "ts-prune이 default/GET/POST/dynamic/revalidate/maxDuration/metadata/viewport 등을 대량으로 '미사용'으로 표시(예: src/app/layout.tsx, src/app/page.tsx, src/app/api/cron/**/route.ts, src/app/reports/[type]/page.tsx 등)",
      "after": "변경 없음",
      "reason": "Next.js App Router 프레임워크 규약으로 암묵 호출되는 export들(default export, route handler GET/POST, 라우트 설정 dynamic/revalidate/maxDuration, 파일 컨벤션 metadata/viewport). 태스크 브리프의 알려진 false-positive 트랩과 정확히 일치 — 실제로는 죽은 코드가 아님."
    },
    {
      "file": "다수 파일 (src/lib/**.ts, src/lib/scoring/**.ts의 '(used in module)' 태그 항목들)",
      "kind": "skipped",
      "before": "예: src/components/SiteHeader.tsx SitePage, src/lib/comprehensive-report.ts buildComprehensiveReportPrompt, src/lib/event-outcomes.ts EventOutcome, src/lib/period-report.ts isReportDay/PeriodStepScoreRow, src/lib/pipeline.ts DailyPipelineResult, src/lib/text-similarity.ts significantTokens/jaccardSimilarity, src/lib/verdict-outcomes.ts anchorIndex, src/lib/scoring/types.ts 등 다수 — ts-prune이 '(used in module)' 태그로 표시",
      "after": "변경 없음",
      "reason": "'(used in module)' 태그는 해당 export가 같은 파일(모듈) 내부에서 실제로 참조되고 있다는 뜻 — 즉 죽은 코드가 아니라 export 키워드만 외부에 안 쓰이는 것뿐. grep으로 표본 확인(SitePage, DailyPipelineResult 등) 결과도 실제 참조가 있어 일치. export 키워드 제거는 삭제가 아니라 리팩터링이라 이번 태스크 범위(삭제) 밖."
    },
    {
      "file": "next.config.ts / prisma.config.ts / vitest.config.ts (default export)",
      "kind": "skipped",
      "before": "ts-prune이 각 설정 파일의 default export를 미사용으로 표시",
      "after": "변경 없음",
      "reason": "Next.js/Prisma/Vitest가 각자 설정 파일의 default export를 도구 자체적으로 로드하는 프레임워크 규약 — 코드 내 import 그래프에는 안 잡히지만 죽은 코드 아님."
    },
    {
      "file": "package.json dependencies: @prisma/client, pg",
      "kind": "skipped",
      "before": "depcheck가 미사용 dependency로 표시",
      "after": "변경 없음",
      "reason": "grep 확인 결과 @prisma/client는 src/generated/prisma/**(prisma generate 산출물, gitignore 대상이라 depcheck가 못 봄)가 직접 import함 — 실사용 확인. pg는 src/ 어디서도 직접 import하는 코드는 없지만, src/lib/db.ts가 쓰는 @prisma/adapter-pg(PrismaPg)가 pg를 peerDependency로 요구하는 구조라 package.json에 직접 명시해둔 것으로 보임(빌드 로그에도 pg-connection-string SSL 경고가 실제로 출력됨, adapter가 pg를 내부적으로 로드하고 있다는 방증) — depcheck가 간접/피어 의존성 사용을 못 잡은 false positive로 보임. 어쨌든 의존성 제거는 이번 태스크 범위 밖이라 실사용 여부와 무관하게 건드리지 않음."
    },
    {
      "file": "package.json devDependencies: @tailwindcss/postcss, @types/pg, @types/react-dom, tailwindcss, tsx",
      "kind": "skipped",
      "before": "depcheck가 미사용 devDependency로 표시",
      "after": "변경 없음",
      "reason": "grep 확인 결과 tailwindcss/@tailwindcss/postcss는 postcss.config.mjs·src/app/globals.css에서 실제로 참조됨. tsx(devDependency)는 npm 스크립트에 등록돼 있진 않지만 scripts/ 아래 다수의 1회성 백필/시뮬레이션 스크립트를 `npx tsx`로 직접 실행하는 용도로 보임(package.json 스크립트에는 없어 depcheck가 놓친 것으로 추정, 완전한 실사용 증거는 못 찾음 — 애매해서 삭제 대상으로 안 다룸). @types/pg·@types/react-dom은 각각 pg·react-dom 타입 보강용 devDependency로 코드에서 직접 import되진 않지만(타입 전용 패키지 특성상 원래 그럼) 대응하는 런타임 패키지가 실사용 중이라 타입 패키지만 따로 죽었다고 보기 어려움. 의존성 제거는 이번 태스크 범위 밖이라 실사용 여부와 무관하게 건드리지 않음."
    },
    {
      "file": "src/lib/notion-write.ts, scripts/fix-0812-bigtech-reasons.ts, scripts/fix-0818-0820-bigtech-reasons.ts",
      "kind": "skipped",
      "before": "eslint @typescript-eslint/no-explicit-any 에러 + notion-write.ts의 사용되지 않는 eslint-disable 주석 2건",
      "after": "변경 없음",
      "reason": "죽은 코드가 아니라 타입 안전성(any 사용) 관련 lint 이슈 — 이번 태스크는 죽은 코드 삭제가 목적이라 범위 밖."
    },
    {
      "file": "src/app/**/*.tsx (커스텀 폰트 경고 다수)",
      "kind": "skipped",
      "before": "@next/next/no-page-custom-font 경고 다수",
      "after": "변경 없음",
      "reason": "죽은 코드가 아니라 폰트 로딩 방식 관련 경고 — 이번 태스크 범위 밖."
    },
    {
      "file": "src/lib/bigtech-reasons.ts",
      "kind": "bug",
      "before": "LLM이 방향과 모순되는 원인을 지어내도(예: 하락했는데 '목표주가 상향') 그대로 저장됨",
      "after": "LLM에 direction 필드도 요청해 changePct1d 부호와 비교, 불일치하면 '명확한 원인 확인 안 됨'으로 대체",
      "reason": "트래커.md에 2026-08-14부터 열려있던 이슈(MSFT/AMZN 실사례로 확인된 환각) 수정"
    },
    {
      "file": "src/lib/bigtech-direction.ts (Task 4 당시엔 src/lib/bigtech-reasons.ts 내부)",
      "kind": "bug",
      "before": "direction 불일치 시 대체 문구가 \"명확한 원인 확인 안 됨(방향 불일치로 제외)\"였다 — src/lib/scoring/run.ts:350과 run.ts:1379가 정확 일치(exact-match, === 비교)로 기대하는 문자열은 접미사 없는 \"명확한 원인 확인 안 됨\"뿐이라, 이 대체 문구가 나오면 두 exact-match 분기 모두 안 타서 \"원인이 있다\"는 걸 전제한 서술(예: 라벨+등락률을 보여준 뒤)이 이어지면서 실제로는 접미사 붙은 정체불명 문구가 그대로 노출되는 자기모순 문장이 만들어졌다.",
      "after": "접미사를 제거해 run.ts의 exact-match 문자열과 정확히 일치시켰다(\"명확한 원인 확인 안 됨\", 접미사 없음). run.ts는 건드리지 않음 — 애초에 Task 4 계획 문서(2026-08-24-codebase-audit-cleanup.md)의 audit-findings 예시 항목도 접미사 없는 문자열을 의도했던 것으로 보이나, 실제 구현 코드(Step 6)에는 접미사가 붙은 채로 들어가 있었다.",
      "reason": "전체 브랜치 리뷰(2026-08-25)에서 발견 — Task 4 검증 당시 checkDirectionConsistency 단위 테스트만 있고 대체 문구를 실제로 조립하는 로직(현재 buildConsistentReasons)에는 테스트가 없어 이 불일치가 개별 태스크 리뷰를 통과했다. bigtech-direction.test.ts에 정확 문자열을 assert하는 회귀 테스트를 추가함(\"exact-match 회귀 테스트\" 표기)."
    },
    {
      "file": "src/components/StepCard.tsx",
      "kind": "spaghetti",
      "before": "MetCell의 충족/불충족(✓/✕) 색이 하드코딩된 Tailwind 기본 팔레트(text-emerald-400, text-rose-400)",
      "after": "text-[var(--pos)] / text-[var(--neg)]로 교체(라이트·다크 모드별 값이 다른 사이트 디자인 토큰)",
      "reason": "site.module.css가 --pos/--neg를 라이트(#1f9d5c/#d5453f)·다크(#4fbf82/#ef8b8b) 테마별로 다르게 정의하는데, 이 스팬만 테마와 무관한 고정 hex(Tailwind emerald-400/rose-400)를 써서 사이트 전체가 지키는 디자인 토큰 규칙(ScoreBadge.tsx 20행 주석에 명시된 원칙, 이미 한 번 지적·수정된 전례)을 놓치고 있었다. 같은 파일 안에서 다른 모든 색(--border, --ink 등)은 이미 토큰을 쓰고 있어 이 두 줄만 예외였다 — 순수 텍스트 색 교체라 표시 결과(✓/✕ 자체)는 동일, 색상 소스만 테마 인지형으로 바뀜. tsc/lint/test 통과 확인."
    },
    {
      "file": "src/components/ReportView.tsx",
      "kind": "spaghetti",
      "before": "1단계 예상밖결과 경고 박스(recentEventOutcomes)와 3단계 경고 문구(step3.warning)가 bg-rose-500/10, text-rose-300/400 등 하드코딩된 Tailwind 기본 팔레트 사용",
      "after": "bg-[var(--risk-high-bg)] / text-[var(--risk-high-text)]로 교체 — 같은 파일이 렌더링하는 RiskyNewsList.tsx가 이미 완전히 동일한 '위험 경고 박스' 패턴에 쓰고 있는 라이트/다크 전용 토큰과 통일",
      "reason": "RiskyNewsList.tsx 자체 주석(9~13행)에 '다크에서는 반투명 오버레이가 잘 보이지만 라이트(흰 배경)에서는 같은 값이 거의 안 보여서(실제 확인) --risk-high-bg/--risk-high-text로 뽑았다'는 전례가 명시돼 있는데, ReportView.tsx의 이 두 지점은 같은 시각적 패턴(옅은 rose 배경 + rose 텍스트의 위험 경고 박스)이면서 그 교훈이 반영되지 않은 채 남아 있었다 — 라이트 모드에서 동일한 저대비 문제가 재현될 위험. site.module.css에 이미 --risk-high-bg/--risk-high-text가 라이트·다크 값 모두 정의돼 있어(21~24행, 49~52행) 새 토큰 도입 없이 기존 값으로 교체. 순수 색상 소스 교체라 레이아웃·문구·조건부 렌더링 로직은 그대로. tsc/lint/test 통과 확인."
    },
    {
      "file": "src/app/**/page.tsx (Google Fonts <link> 3줄 + PageShell 스타일 wrapper div가 8개 파일에 거의 동일하게 반복됨)",
      "kind": "skipped",
      "before": "page.tsx, report/page.tsx, calendar/page.tsx, calendar/[date]/page.tsx, indicators/page.tsx, news/page.tsx, reports/[type]/page.tsx, reports/[type]/[start]/page.tsx 8개 파일이 각자 <link rel=preconnect>×2 + <link rel=stylesheet> 3줄과 --font-gothic/--font-sans 인라인 style을 가진 wrapper div를 거의 동일하게 반복",
      "after": "변경 없음",
      "reason": "브리프의 '3회 이상 반복 로직' 스파게티 기준에 해당하는 걸로 보이나, 통합하려면 공용 PageShell/레이아웃 컴포넌트를 새로 만들어 8개 파일 전부를 그걸 쓰도록 고쳐야 한다 — 이는 태스크 브리프의 'Code Organization' 지침(\"컴포넌트를 쪼개는 게 '맞는' 해법이어도 이번 태스크에서 독단적으로 하지 말고 스킵/우려사항으로 남겨라\")이 명시적으로 범위 밖이라고 한 리스트럭처링에 해당해 손대지 않음. eslint @next/next/no-page-custom-font 경고도 이미 이전 태스크(현재 items 배열 상단, \"src/app/**/*.tsx (커스텀 폰트 경고 다수)\" 항목)가 같은 패턴을 '폰트 로딩 방식 관련 경고, 범위 밖'으로 스킵 처리한 전례가 있다."
    },
    {
      "file": "vitest.config.ts",
      "kind": "bug",
      "before": "defineConfig({ resolve: { alias: {...} } }) — .env/.env.local을 읽어 process.env에 채우는 로직이 전혀 없었다. src/lib/bigtech-reasons.ts(Task 4에서 추가, checkDirectionConsistency 7개 테스트 보유)가 src/lib/db.ts를 transitively import하는데, db.ts는 모듈 로드 시점에 process.env.DATABASE_URL이 없으면 즉시 throw한다 — DATABASE_URL은 .env/.env.local에 실제로 있었지만 vitest 설정이 그 파일들을 읽지 않아, `npm run test`(플레인, 특수 플래그 없음)로 돌리면 src/lib/bigtech-reasons.test.ts 전체가 import 단계에서 에러 나며 0 tests collected로 스킵됐다 — \"0 failing\"이 아니라 파일 자체가 안 실렸다는 뜻이라 눈에 띄지 않고 조용히 회귀 커버리지가 0이 되는 문제였다(Task 5 리뷰어가 npm run test를 직접 돌려 발견).",
      "after": "vitest.config.ts는 원래의 최소 형태(defineConfig({ resolve: { alias: {...} } }))로 되돌렸다 — loadEnv로 .env를 읽어 채우는 접근은 전체 브랜치 리뷰에서 CI를 깨뜨리는 걸로 드러나 폐기(.github/workflows/test.yml은 시크릿을 전혀 설정하지 않고 npm test를 돌리도록 의도적으로 설계돼 있어 — DB·외부 API 접근이 없는 순수 함수 테스트라는 전제 — .env 자체가 CI엔 없다). 대신 근본 원인(순수 판정 로직이 DB 클라이언트를 transitively import)을 없앴다: checkDirectionConsistency와 결과 조립 로직(buildConsistentReasons로 명명)을 새 파일 src/lib/bigtech-direction.ts로 옮겼다 — @/lib/db, @/lib/metrics 등 DB를 건드리는 어떤 것도 import하지 않는 순수 모듈. bigtech-reasons.ts는 이제 이 두 함수를 가져다 쓰기만 하고, bigtech-reasons.test.ts(구 위치)는 삭제, 테스트는 bigtech-direction.test.ts로 이전 — 이 테스트 파일은 db.ts를 아예 import하지 않으므로 DATABASE_URL 유무와 무관하게 항상 실행된다. news-events.ts가 news-severity.ts를 분리한 것과 동일한 패턴(파일 14행 주석: \"순수 판정 로직은 news-severity.ts로 옮겼다(테스트가 DB 없이 돌아야 해서)\").",
      "reason": "전체 브랜치 리뷰(2026-08-25)에서 Critical로 지적됨: loadEnv 방식은 로컬에 .env가 있을 때만 동작하고 CI(.env 없음)에서는 여전히 DATABASE_URL 미설정으로 bigtech-reasons.test.ts import가 throw해 CI가 빨간불이 된다는 걸 재현으로 확인함. 모듈 분리 후 .env/.env.local을 모두 임시로 치우고(CI 시뮬레이션) `npm run test`를 돌려 13개 파일·112개 테스트 전부 통과, DB 관련 에러 0건을 확인 — 이게 이 수정이 실제로 CI를 깨지 않는다는 결정적 증거."
    },
    {
      "file": "src/lib/sources/krx-short.ts",
      "kind": "skipped",
      "before": "loginKrx()의 warmup1/warmup2/doLogin() 세 fetch 모두 res.ok를 확인하지 않고 바로 쿠키 수집·res.json() 파싱으로 넘어간다(Discord 웹훅과 같은 'res.ok 미확인' 패턴에 해당).",
      "after": "변경 없음",
      "reason": "완전히 조용한 실패는 아니다 — doLogin이 HTML/비JSON 응답을 받으면 res.json()이 SyntaxError로 throw하고, 로그인 자체가 실패하면 result._error_code가 CD001이 아니라서 명시적으로 'KRX 로그인 실패' 에러를 던진다(80~81행) — 어느 경로든 호출부에 에러가 그대로 전파돼 Discord 버그처럼 실패가 사라지진 않는다(에러 메시지가 'HTTP 500' 대신 파싱 에러로 나오는 수준의 차이). 이 파일 자체가 파일 상단 주석에서 '사용자가 위험을 인지하고 승인함'이라고 명시한 비공식 로그인 세션 스크레이핑(data.krx.co.kr, pykrx 흐름 재현)이라 로그인 흐름을 건드리면 실제 KRX 세션 발급이 깨질 위험이 있고, 수정해도 에러 메시지 품질만 개선될 뿐 동작은 바뀌지 않아 '안전하게 확인 가능한 버그'보다는 리스크가 큰 변경이라 스킵."
    },
    {
      "file": "src/lib/news-market-events.ts",
      "kind": "skipped",
      "before": "FINRA_SHORT_VOLUME_URL / DART_EQUITY_URL / DATAROMA_URL / OPENINSIDER_URL 4개 상수가 src/lib/scoring/run.ts의 step7 상세 표가 쓰는 동일 URL과 값이 겹친다(중복 로직 후보).",
      "after": "변경 없음",
      "reason": "파일 자체 주석(17~18행)에 'run.ts의 step7 상세 표가 쓰는 것과 동일한 실제 출처 링크(하드코딩 상수 재선언 — run.ts는 export 안 해서 그대로 복제, 값이 바뀌면 두 곳 다 고쳐야 함을 감수)'라고 이미 의도적 트레이드오프로 명시돼 있음. 공용 상수 파일로 뽑으려면 run.ts(scoring/** 제외 대상)를 같이 고쳐야 해서 이번 태스크 범위 밖이라 스킵."
    }
  ],
  "engine_explainers": [
    {
      "fn": "runDailyAnalysis",
      "file": "src/lib/scoring/run.ts:815",
      "explain_kr": "이 함수는 사이트에 매일 올라오는 '오늘의 리포트' 점수 계산을 전부 담당한다. 1단계(뉴스·이벤트 위험으로 인한 거부권)부터 8단계(최종 '매수/지켜보기/현금비중 늘리기' 결론)까지 8단계를 정해진 순서대로 계산하면서, 각 단계에서 나온 숫자와 판정을 다음 단계·최종 종합판단·리포트 화면(표, 요약 문장, 슬라이드 자료)에 계속 쌓아서 넘긴다. 한마디로 '오늘 들어온 시장 데이터를 넣으면 오늘의 결론과 그 근거표가 나오는' 이 사이트의 심장 같은 함수다.",
      "risk_kr": "이 함수를 손대면 위험한 구체적인 이유가 코드에 그대로 보인다. 첫째, 맨 마지막 8단계 점수(scoreStep8, 1517행)는 1~7단계에서 계산해 둔 결과 객체를 통째로 받아 계산하므로, 중간 어느 단계 하나라도 계산 순서나 반환 형태를 바꾸면 최종 점수 전체가 깨진다. 둘째, 2단계에서 만든 netLiq·rrpStatus·creditSpreadBp 같은 변수들이 코드 훨씬 뒤쪽인 forwardSignals(1536~1543행, 리포트의 '앞으로 자본이 이동할 곳' 문단 재료)에서 다시 재사용된다 — 즉 앞부분 2단계 코드를 건드리면 얼핏 상관없어 보이는 맨 뒤 문단까지 조용히 깨질 수 있다. 셋째, 코드 곳곳에 실제로 있었던 버그를 고친 흔적이 주석으로 남아 있다(예: 미국채-일본채 스프레드를 미국 자체 2Y-10Y 버킷에 잘못 섞어 넣어 항상 100%ile로 나오던 버그, 뉴스 위험 거부권이 21일 중 15일이나 과민하게 발동하던 문제, 이미 발표된 경제지표가 '지난 발표 결과'와 '14일 내 예정' 두 목록에 동시에 뜨던 자기모순 등) — 이런 사례들은 이 함수가 실제로 여러 차례 미세한 로직 실수가 라이브 리포트에 그대로 반영된 전례가 있다는 뜻이다."
    },
    {
      "fn": "runDailyPipeline",
      "file": "src/lib/pipeline.ts:59",
      "explain_kr": "매일 자동으로(크론) 실행되는 전체 파이프라인이다. 순서대로 1) FRED·Yahoo·CFTC·코인게코 등 10여 개 외부 데이터 소스에서 오늘치 시장 데이터를 동시에 받아오고, 2) 오늘이 이미 리포트를 만든 날(주말·휴장일 중복)인지 먼저 판단하고, 3) 아니면 runDailyAnalysis를 불러 점수를 계산시키고, 4) 계산 결과에 LLM이 쓴 해설 문장까지 붙여 데이터베이스에 저장하고, 5) 옵시디언 백업·디스코드 알림·노션 기록을 차례로 처리한 뒤, 6) 주간/월간/분기/연간 리포트가 마감일이면 그것도 만든다. 이 사이트가 매일 새벽 자동으로 새 리포트 하나를 완성해내는 전체 과정을 지휘하는 함수다.",
      "risk_kr": "이 함수도 손대면 위험한 구체적 근거가 코드와 주석에 남아 있다. 첫째, 69~92행에서 Promise.allSettled로 병렬 호출한 배열의 순서와, 그 결과를 받는 구조분해할당(fredResult, fredMonthlyResult, cftcResult…)의 순서가 정확히 일치해야 한다 — 소스 하나를 배열 중간에 추가/삭제하면서 구조분해 순서를 안 맞추면 타입 에러 없이 조용히 엉뚱한 데이터가 엉뚱한 변수에 들어간다. 둘째, marketDate(리포트가 실제로 다루는 거래일)를 구할 때 find()가 아니라 최댓값을 찾아야 한다고 주석(167~171행)에 실제 사고 사례가 적혀 있다 — Yahoo가 날짜를 오래된 순서로 주는데 find()를 썼다가 최신이 아니라 가장 오래된 날짜를 집어버린 라이브 버그가 실제로 있었다. 셋째, 휴장일 중복 판정(isRepeatMarketDay)은 반드시 무거운 계산(빅테크 등락원인 LLM 호출, 기관매집 스크레이핑 등) '이전'에 해야 한다고 184~188행 주석에 명시돼 있다 — 원래 이 판정이 무거운 계산 뒤에 있어서 휴장일마다 API 호출이 낭비되던 문제를 고친 이력이다. 넷째, 205~211행 주석에는 '오늘자 새 리포트 생성'만 건너뛰고 6)번 주기별 리포트 생성은 반드시 그 조건문 밖에서 항상 실행돼야 한다고 적혀 있다 — 예전에 이 둘을 같이 묶었다가 일요일(항상 미국장 휴장이라 매주 스킵 대상)마다 주간 리포트가 영구히 생성되지 않는 회귀가 실제로 발생했다. 다섯째, narrative 생성(LLM 호출) 직후 정확히 20초(sleep(20_000), 356행)를 기다린 뒤 comprehensiveReport를 생성한다 — 이 간격을 줄이거나 순서를 바꾸면 무료 LLM API의 분당 요청 제한(429 레이트리밋)에 걸린다는 설명이 주석에 있다. 이런 사례들은 전부 '순서를 한 번 잘못 바꿨다가 실제 라이브에서 걸려 나중에 고친' 흔적이라, 겉보기엔 단순한 재배치도 같은 종류의 회귀를 다시 낼 위험이 크다."
    }
  ]
}
